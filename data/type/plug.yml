type: "fan"
commands:
  set_power:
    description: "Set power on/off"
    parameters:
      - string_on_off
properties:
  power:
    description: "Current device power state"
    results:
      - string_on_off
  temperature:
    description: "Current plug internal temperature."
    results:
      - type: "number"